rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development Sandbox: Allow all reads and writes for wireless mobile app testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
